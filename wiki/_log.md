# Wiki log

Chronological changelog of wiki updates. One line per page added or significantly changed, in
order of when the entry was written. Newest entries at the bottom.

Format: `- [<title>](<path>) — <category> — <one-sentence summary>`

- 2026-07-22 — Seed wiki bootstrap — meta — Wiki structure scaffolded from `wiki-seed.md`; awaiting first real entries.
- 2026-07-22 — Backlog — meta — Parked Phase 1 out-of-scope items (soundtrack coverage, authentic art, SFX, i18n, error recovery, hosting/deploy, E2E) in `backlog.md`.
- 2026-07-22 — Audio pipeline — decision — Editorial `tracks.ts` SOT + committed generated `audio-manifest.json`; MP3s gitignored; CI-safe coverage test — commit `✨ add track data + audio build pipeline`.
- 2026-07-22 — Audio hosting — integration — `VITE_AUDIO_BASE_URL` contract for serving MP3s (dev `/audio/`, prod CDN; CORS + range) — commit `✨ add track data + audio build pipeline`.
- 2026-07-22 — Audio engine — decision — Native `HTMLAudioElement` (not Howler); gesture-gated play, repeat-one handled imperatively — commit `✨ add player engine + state`.
- 2026-07-22 — Player state — decision — Zero-dep `useReducer` + Context; `currentTime` tick kept local to ProgressBar (overrides Zustand rec) — commit `✨ add player engine + state`.
- 2026-07-22 — Theming — decision — Two-tier CSS custom properties on `[data-theme]` (Good/Evil); pre-paint hydration; CSS-recreated GameButton/GameFrame — commit `💄 add theming + in-game UI primitives`.
- 2026-07-22 — Architecture — reference — Filled the module map, layered model, and "play a track" worked example — commit `📝 populate wiki architecture + glossary`.
- 2026-07-22 — Glossary — reference — Defined domain terms: Good/Evil theme, category, track id, manifest, epoch, scrubbing, VITE_AUDIO_BASE_URL — commit `📝 populate wiki architecture + glossary`.
- 2026-07-22 — Music player — feature — Documented the single-screen player; noted the track list was removed (navigation via transport) — commit `♻️ address solution-critic findings`.
- 2026-07-22 — Castle labeling fix — bug — Repointed base castle `src` to authoritative fheroes2 mapping (homm2_04=Sorceress…09=Wizard); added `src→title` guard test — commit `🐛 fix mislabeled castle themes`.
- 2026-07-22 — Extended soundtrack — decision/feature — Added 6 Succession Wars castle alternates + 20 event stings (45 tracks); stings behind a scope-chip filter; manual next/prev now wrap — commit `✨ add … + category scope`.
- 2026-07-22 — Glossary/architecture/backlog — reference — Added sting/scope/SW-variant terms, `scope` in the reducer model, struck completed backlog items — same commits.
- 2026-07-22 — Castle theme mislabel — bug — Postmortem: base castles mapped to wrong OGGs since Phase 1 (bad WebFetch summary + false "verified" comment); fixed + guard test — commit `📝 add castle-mislabel postmortem`.

- 2026-07-23 — UI art extraction — integration — AGG/ICN→PNG extractor (ported fheroes2 decode), role map + committed art-manifest + bijection test — commit `✨ add AGG/ICN art extraction pipeline`.
- 2026-07-23 — Authentic UI reskin — feature — Frame (SURDRBKG/E border-image, good=round/evil=stretch, baked shadow cropped) + game button faces via asset tokens; viewport page-frame; neutral album inset; de-reddened evil bg — commit `💄 reskin frame + buttons with extracted art`.
- 2026-07-23 — UI art decision — decision — Extract HOMM2 sprites (border-image frame, per-theme repeat, cropped shadow, button faces); responsive; font deferred — commit `📝 wiki: authentic UI art`.
- 2026-07-23 — Glossary — reference — Added AGG, ICN, Asset manifest terms — commit `📝 wiki: authentic UI art`.
- 2026-07-23 — Address solution-critic — refactor — Pure decode split to scripts/icn.ts + unit tests; pruned dead tokens; hex→--inset token; frame fallback bg; coarse-pointer 44px targets — commit `♻️ address solution-critic findings`.
- 2026-07-23 — Authentic HOMM2 fonts — integration — Build TTF webfonts from the game's own bitmap `FONT.ICN`/`SMALFONT.ICN` via opentype.js (`yarn extract:font`); titles + chips now render the real typeface — commit `💄 polish authentic UI: fonts, page frame, cursor, pixel icons`.
- 2026-07-23 — Ornate page frame — decision — Added `page` asset role (`ADVBORD`/`ADVBORDE`) for the viewport border; cropped off the right control panel (`trim.right: 160`) so the wood-and-gem border tiles cleanly with `round` — same commit.
- 2026-07-23 — Transport icons — feature — Made all transport buttons uniform size with smaller (44%) glyphs per feedback — same commit.
- 2026-07-23 — Cursor + pixel-art icons — feature/decision — Added `cursor` asset role (ADVMCO#0, ×2 upscale via new `scale`) wired to a per-theme `--cursor`; redrew transport/settings icons as 12×12 pixel-art SVGs (filled triangles, crossing-arrow shuffle, monitor+keyboard settings); increased panel spacing and made the now-playing category gold/legible — commit `💄 polish authentic UI: fonts, page frame, cursor, pixel icons`.
- 2026-07-23 — Shared AGG reader — refactor — Hoisted duplicated `openAgg` into `scripts/agg.ts`, reused by both extractors; removed dead `lg` GameButton size variant (solution-critic findings) — same commit.
- 2026-07-23 — Font crispness + gaps — bugfix/feature — Font glyphs were muddy: fixed by merging each glyph row's pixels into one rect per run (no internal seams) instead of per-pixel squares; widened chip + transport horizontal gaps — commit `💄 crisp font glyphs + wider player gaps`.
- 2026-07-26 — Font shadow bug — bugfix — HOMM2 font glyphs rendered doubled/unreadable: the builder's `isOpaque` (`alpha > 128`) was filling the baked drop-shadow (alpha ~178) into each glyph; tightened to `> 200` so only the solid letter is used — commit `💄 authentic UI: font shadow fix, game arrows, chips, scroll-bar, gold text`.
- 2026-07-26 — Game arrow icons — feature — Play/prev/next now use the game's own up-arrow sprite (`arrow` role, `RECRUIT`#0) rotated via new pure `rotateSprite` + extractor `rotate`; transport row tinted gold — same commit.
- 2026-07-26 — Player spacing — feature — Real inter-element spacing moved onto `.game-frame__inner` (flex column, gap 16px); the `.player` gap was a no-op (single child); widened chip/transport gaps; larger, more legible font sizes — same commit.
- 2026-07-26 — Game-authentic controls — feature — Scope chips restyled as the game's beige map-size buttons (active = darker pressed face, centred `#6c4327` text); progress bar restyled as a horizontal game scroll bar (black groove + beige grip); transport glyphs + arrow tinted dark brown `#552b0d` (new pure `tintSprite` + extractor `tint`); display text fixed gold `#dcc132`; uppercase dropped — commit `💄 authentic UI: font shadow fix, game arrows, chips, scroll-bar, gold text`.
- 2026-07-26 — Dialog spacing + tests — fix — Moved inter-element spacing to `.game-frame__inner` flex gap and dropped the settings dialog's redundant child margins (double-spacing); added `rotateSprite` 180/270 + `tintSprite` unit tests (solution-critic) — same commit.
- 2026-07-26 — Button/slider refinements — feature — Chips + Close redrawn as pure-CSS beige buttons (symmetric bevel, centred labels, active = `#b7885e` inset) — fixes the sprite-bevel off-centering; slider grip now sits inside a shorter groove and is wider; removed time labels; buttons gained a drop shadow; evil theme greys (buttons, `#505050` glyph/label/arrow ink, grey grip) — commit `💄 css text-buttons, inside-groove slider, evil greys`.
- 2026-07-27 — Revert chips to sprite buttons — refactor — Per user, scope chips + Close reverted from the pure-CSS buttons back to the `--btn-face` sprite (border-image), matching the transport buttons; active = darkened; removed the now-unused `--btn-beige/hi/sh/active` tokens — commit `⏪ revert text-buttons to game-sprite style`.
- 2026-07-27 — Game settings icon + theme creatures — feature — Settings button now uses the game's real System-Options computer glyph (ADVBTNS #14, lifted via new `keyLuma` transform + tint); theme toggle uses game creatures (Phoenix / Black Dragon, MONS32); body+title text turned near-white; icon buttons share the text-buttons' darken active style; glyphs enlarged; chips uppercased with fixed height — commit `✨ game settings/creature icons, white text, bigger glyphs`.
- 2026-07-27 — Swap theme creatures — tweak — Good = Black Dragon, Evil = Phoenix (swapped) — commit `🔧 swap theme-toggle creatures`.
- 2026-07-27 — Game album covers — feature — Album art now uses real game elements per track: town songs show the faction castle building (TWN?CSTL) via a new un-themed `COVERS` map + `cover-manifest.json` + bijection test; extractor transform chain refactored into `applyTransforms`; other categories keep the SVG emblem — commit `✨ album covers from game art (faction castles)`.
- 2026-07-27 — Covers: map castles + category creatures — feature — Town covers switched to the adventure-map castle (MINITOWN 0-5) per user ("castles from the map"); added covers for battle/terrain/menu/victory/sting (MONS32 creatures); AlbumArt resolves id-then-category — commit `🎨 map-castle + creature album covers`.
- 2026-07-27 — Town covers: castle only — tweak — Town covers use the faction castle building sprite (TWN?CSTL, no scenery) per user; the full atmospheric map scene is not a single extractable asset — commit `🎨 town covers: castle sprite only`.
- 2026-07-27 — Town covers: drop-in — feature — Adventure-map castle cannot be reassembled from OBJNTOWN (tile grid layout not in the ICN); AlbumArt now loads /art/covers/<id|category>.png with on-404 fallback so per-faction castle screenshots dropped at town-<faction>.png are used automatically; category creature covers kept — commit `♻️ drop-in town covers + on-error cover fallback`.
- 2026-07-27 — Town covers: top-tier creatures — feature — Town songs now show the faction top-tier upgraded creature (Crusader/Cyclops/Phoenix/Black Dragon/Titan/Bone Dragon) from MONS32; drop-in override still supported — commit `🎨 town covers: top-tier faction creatures`.
- 2026-07-27 — Hero toggle, shield menu, fixed-height title — feature — Theme toggle now shows campaign hero portraits (Roland/Archibald, role renamed creature→hero); menu+victory covers = heraldic shield, battle=Nomad, terrain=Rogue, sting=Ghost; title is single-line + marquee for a fixed-height player; removed button hover effects; evil scroll-bar border greyed — commit `✨ hero theme toggle, shield covers, fixed-height marquee title`.
- 2026-07-27 — Swap hero toggle — tweak — Theme toggle shows the target hero: Good→Archibald, Evil→Roland — commit `🔧 swap theme-toggle hero portraits`.
- 2026-07-27 — Evil album border grey — tweak — Album-art frame ring uses `--art-border` (gold in Good, grey in Evil) — commit `💄 grey album-art border in evil theme`.
- 2026-07-27 — Start gate — feature — HOMM3-style fog-of-war intro modal; horse Start button (ADVBTNS #2 `horse` role) reveals/unblurs the map and begins playback (the click is the autoplay gesture) — commit `✨ fog-of-war start gate + horse button`.
- 2026-07-27 — Start gate → two steps — feature — Split the intro: (1) black screen + welcome + Okay reveals/unblurs the map; (2) compact horse panel with a "Start" label; the player stays hidden until the horse is pressed — commit `💄 two-step start gate: okay → horse, hidden player`.
- 2026-07-27 — Fundraising widget + legal footer — feature — Bottom-right treasure-chest "Server fundraising" button opens a modal linking to Buy Me a Coffee ("Buy me a brick"); bottom-left footer opens Copyrights & Privacy modals (HOMM2-adapted text, Sevalla host, fair-use note); chest asset role OBJNRSRC #19 — commit `✨ add fundraising widget + legal footer`.
- 2026-07-27 — Shared Dialog primitive — refactor — Extracted `src/ui/Dialog.tsx` (backdrop + framed panel + title + Close + Escape-to-close + focus trap); SettingsDialog now shares it instead of hand-copying the skeleton — commit `♻️ extract shared Dialog primitive with focus trap`.
- 2026-07-27 — Cursor on page edges — bugfix — Game pointer reverted to the OS arrow on the bottom/right edges: (1) moved `--cursor` from `body` to `html` (edge hit-testing falls through the pointer-events:none frame to the root), and (2) scaled the cursor ×1.5→23×32 (was ×2→30×42) to stay under Chromium's 32px anti-spoofing cap that drops edge-overflowing custom cursors; needed integer rounding in `scaleSprite`. See `bugs/2026-07-27-cursor-edges.md` — commit `🐛 fix game cursor reverting to default at page edges`.
- 2026-07-27 — Static + audio caching — config — Added `public/_headers` (immutable long-cache for hashed `/assets/*` + `/fonts/*`, 7-day `/art/*`, revalidate `index.html`) and documented the versioned-prefix + per-object `Cache-Control` strategy for the Sevalla audio bucket in `.env.example` + `integrations/audio-hosting.md` — commit `⚡ add static + audio cache headers`.
- 2026-07-28 — Black Dragon favicon — feature — `extract-art.ts` now also writes `public/favicon.png` (the `town-warlock` Black Dragon cover centered on a transparent square via new `padToSquare`); wired into `index.html` as `rel="icon"` + `apple-touch-icon`; gitignored like other extracted art — commit `💄 add black dragon favicon`.
- 2026-07-28 — Scheme-adaptive favicon — feature — Added a dark-mode favicon: `extract-art.ts` also writes `public/favicon-dark.png` (the `town-sorceress` Phoenix, bright and legible on dark chrome) alongside the Black Dragon `favicon.png`; `index.html` selects per `prefers-color-scheme` via `media` on the icon links — commit `💄 scheme-adaptive favicon (phoenix on dark)`.
