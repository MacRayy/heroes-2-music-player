# Base URL where transcoded MP3 audio is served from.
# Local dev leaves this unset and serves from /audio/ (public/audio/).
# In production point it at your object storage / CDN bucket, e.g.:
# VITE_AUDIO_BASE_URL=https://cdn.example.com/homm2-audio/
VITE_AUDIO_BASE_URL=
